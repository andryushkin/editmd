# TODO

Разбор бэклога 2026-07-17: каждому пункту дан вердикт (в план / отложено / переосмыслено),
из них + хвостов CLAUDE.md и прошлых спринтов собран общий план. Детали завершённого —
в `docs/HISTORY.md`.

## Вердикты по старому бэклогу

| Пункт | Вердикт |
|---|---|
| Комментирование через тег-комментарий (YAML в комменте) | **Переосмыслено** → объединено с «закладками» в один концепт «якоря и комментарии» (спринт 7, сначала дизайн) |
| Формулы — хвосты спринта | **В план** (спринт 2) |
| Workspace: модель закрепления | **В план** (спринт 1), предложен дефолт без настройки |
| Закладки как скрытые HTML-комментарии | **Переосмыслено** → block-ID `^id` уже парсится wiki-link-сканером; HTML-комментарии не нужны (спринт 7) |
| Таблицы: столбцы/строки из UI | **В план** (спринт 1) — только нативные таблицы; island-grid уже умеет всё через контекстное меню |
| Удалить `yamlLineSegments` | **В план** (спринт 1) — потребителей в app-коде нет, проверено |
| Review tombstones | **В план** (спринт 6) — кросс-репо со smotr, отдельный заход |
| URL из буфера → ссылка | **В план** (спринт 1) — только в момент paste, без живой линкификации (тогда «удалил форматирование — не возвращается» получается бесплатно) |

## Проверить глазами (перед спринтом 3)

- [ ] Темы Preview 0.42.0: все 5 тем на документе с формулами/callout/таблицами, оба appearance; палитра в strip
- [ ] Плашки цитат и отступы вложенных блоков Visual (спринт визуальных багов, cfc5d80)
- [ ] Русская локализация UI (4f93da3)

## План спринтов

### 1. Гигиена и быстрые победы — **ЗАВЕРШЁН 2026-07-17** (app 0.43.0)
- [x] Удалить `yamlLineSegments` + хелперы + их тесты из `Frontmatter.swift` (203f9aa)
- [x] Workspace-закрепление — **решено: оставить как есть**. Одиночные файлы вне workspace остаются session-only (закрепление вручную пином); workspace-папки уже персистятся. Авто-запоминание и авто-усыновление папки отклонены.
- [x] Paste URL: голый URL на выделении → `[выделение](url)`; без выделения → plain (725a3d8). Общий `PasteLink.swift`, последняя дверь funnel в Source и Visual, только paste-время
- [x] Нативные таблицы: Add/Delete Column в меню Table в strip (01a21d3); Source и контекстное меню Visual уже умели столбцы — добавлено только в strip
- [x] Per-document режим редактора: `EditorModeStore` (путь→mode), восстановление в ContentView (e0af358)

### 2. Корректность: round-trip и формулы
- [ ] Visual схлопывает пустые строки между пунктами списка (замечено на CLAUDE.md) — воспроизвести, зафиксировать тестом round-trip, починить сериализатор
- [ ] `\$` теряет эскейп при Visual-сериализации (`escapeInline` не эскейпит `$`)
- [ ] Многострочный `$$…$$` внутри blockquote (маска съедает `>`)
- [ ] (наблюдение) «Preview не перерендерился после удаления строки» — не воспроизведено; при повторении вернуть NSLog-цепочку `syncToDocument → updateNSView → render`

### 3. Темы, этап 2 — унификация (после проверки глазами этапа 1)
- [ ] `ThemeSpec` (семантические токены: палитра light/dark, типографика, декорации) + три проекции: Preview CSS (уже есть как `PreviewTheme`), Visual (`EditorTheme` становится производным), Source (только фон+палитра, шрифт всегда mono)
- [ ] Снос Nord/Solarized/Dracula с миграцией id (решение пользователя 2026-07-17); High Contrast остаётся
- [ ] Единый пикер темы (сейчас два: Settings ▸ General для Source/Visual и палитра Preview)
- [ ] Precedence: fallback'и `ElementStyles`/`fontFamily` ведут в тему; кнопка «Reset to theme»

### 4. Изображения
- [ ] Drag-and-drop изображений в Source/Visual (тот же guard и сериализатор `markdownImageSyntax`, что у paste/кнопки)
- [ ] Remote images (`http(s)`) в Visual — рендер с async-загрузкой и кэшем, без блокировки layout

### 5. Поиск внутри Preview
- [ ] ⌘F в режиме Preview: подсветка совпадений в WKWebView (JS-слой), навигация next/prev; связать с существующей моделью поиска Source/Visual

### 6. Review tombstones (кросс-репо со smotr)
- [ ] Маркер удаления в sidecar-схеме (иначе локально удалённая метка воскресает через merge-by-id при гонке с внешним писателем); согласовать схему в `~/Server/smotr`, PR туда + поддержка в `ReviewSidecar.save`

### 7. Якоря и комментарии в тексте (сначала дизайн, потом код)
- [ ] Дизайн-сессия (smotr-прототип): комментарии к блокам + закладки на одной механике; якорь — markdown-нативный `^block-id` (сканер уже понимает), НЕ скрытые HTML-комментарии; чем это отличается от review-меток и когда что использовать
- [ ] Реализация — по итогам дизайна

### 8. Широкие ячейки Visual-grid (большой)
- [ ] Word-wrap внутри колонки + переменная высота строк island-grid (сейчас фикс. rowHeight → пересчёт y-раскладки); большие файлы обязаны открываться в Visual — ограничение пользователя
- [ ] TextKit 2 — НЕ отдельная задача; рассматривать точечно только если wrap упрётся в TextKit 1

## Отклонено / не делаем

- Скрытые HTML-комментарии как механизм закладок — заменено block-ID (`^id`).
- Живая линкификация URL при наборе — только paste-время.
- TextKit 2 как самостоятельный рефактор — только по нужде спринта 8.
