# TODO

Спринт разделов 01–08 (правый inspector документа, ссылочный индекс, wiki-link UX,
Properties-панель, история файла, vault-lint, поиск по workspace, мелочи сайдбаров)
**завершён**. Источник идей — исследование
[raytsystem-public-os](https://github.com/romarayt/raytsystem-public-os) (2026-07-16).
Планы лежат в `docs/plans/NN-*.md` (статусы — в шапках планов), итоги и детали — в
`docs/HISTORY.md`. Follow-up после спринта: frontmatter полностью скрыт со страницы,
плагины (multi-checkbox) и их установка переехали в панель «Свойства».

## 09 — «AI ready»: единая система интеграции агентов

План: `docs/plans/09-ai-ready.md` (статус — в шапке плана). Цель: работа с любым
харнесом (Claude Code, Codex, pi, shell) по лесенке файлы → CLI → skill → адаптеры;
вся AI-интеграция видима в UX. Сквозной принцип: каждый AI-touchpoint ведёт дальше
(подсказки следующего шага, палитра готовых промптов из одного источника со skill).

- [x] Этап 1 — `AgentActivityModel` + индикатор ✨ в тулбаре (popover, промпты, badge)
- [x] Этап 2 — статус-канал `editmdctl agent-status` + пакет хуков (Claude/Codex/pi/shell)
- [x] Этап 3 — skill-пакет в бандле + установка в `~/.claude/skills/` и `~/.codex/skills/`
- [x] Этап 4 — Settings ▸ Integrations (установщики, выбор команды агента для ✈️, env)
- [x] Этап 5 — единый визуальный язык AI-правок (openDiff ↔ suggestions)
- [x] Этап 6 — MCP stdio proxy `editmd-mcp` (опциональный слой; auto-reg в конфиги — follow-up)
- [x] Этап 7 — грациозные внешние правки (auto-reload чистого буфера + toast)

Закрывает пункт бывшего бэклога «Вкладка AI-интеграции в сайдбаре — непонятно,
как работает интеграция с Claude».

## Бэклог (без планов, пока не дозрели)

- [ ] Комментирование текста через тег-комментарий — как визуально встроить, чтобы было понятно, к каким блокам относится (например, в комментарий вставлять YAML)
- [ ] Работа с формулами — хвосты после формулы-спринта
- [ ] Workspace: закреплять автоматически / спрашивать / забывать при следующем запуске — решить модель по умолчанию
- [ ] Закладки внутри файлов в виде скрытых HTML-комментариев (на подумать)
- [ ] Таблицы: вставка столбцов, удаление столбцов и строк из UI
- [ ] Удалить `yamlLineSegments` и его хелперы из `Editor/Frontmatter.swift` — после скрытия frontmatter со страницы у токенизатора не осталось потребителей в app-коде (только тесты); убрать вместе с тестами
- [ ] Review-метки: tombstones для удалений — при гонке с внешним писателем (smotr web / `claude /smotr -pr`) локально удалённая метка воскресает через merge-by-id в `ReviewSidecar.save`. Нужен маркер удаления в sidecar — требует согласования схемы со smotr (`~/Server/smotr`), см. docs/HISTORY.md § v38.1 gotchas
