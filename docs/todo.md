# TODO

## Баги

- [ ] Files-сайдбар: в папке с большим числом файлов выбор файла из середины списка при открытии скачком прокручивает сайдбар к началу папки (2026-07-18). То же самое в поиске по workspace: выбор файла из результатов — скачок прокрутки вверх. Вероятная зона: `WorkspaceSidebar`/`WorkspaceSearchSidebar` — открытие файла перестраивает List (identity строк/структуры меняется при rebuild от workspace-обновления), и скролл сбрасывается; смотреть, что ре-компьютится при open (SidebarSnapshot/FolderInfo/contentEpoch) и стабильны ли id у ForEach.

## Производительность (кандидаты из профилирования 2026-07-18, WoL-вольт 6919 md)

- [ ] Поиск по workspace: горячие следы (`_opaqueCharacterStride`, `String.lowercased`, `Substring.index(before:)`) были сняты, когда параллельно жёг vault-lint — после его фикса (2be2133) перемерить на чистом сценарии, прежде чем оптимизировать. Идея на случай подтверждения: case-insensitive поиск по UTF-8 байтам без lowercased-копий (`WorkspaceSearchModel`/`SearchMatch.swift`).
- [ ] Layout при малой ширине окна: правый инспектор наползает на соседние панели, границы панелей визуально перекрываются (2026-07-18). Вероятная зона: минимальные ширины у сплитов layout’а (ContentView/инспектор) — не заданы/не суммируются `minWidth` панелей, и при сжатии окна сплиты не упираются, а перекрывают друг друга. Проверить на комбинации сайдбар + сплит Source/Visual + Preview + инспектор.
- [ ] Visual: не работает редактирование больших таблиц (2026-07-18, найдено при проверке CPU-фикса LinkIndex на WoL-вольте). Уточнить репро: размер таблицы и что именно не работает (ввод в ячейку? сериализация?). Вероятная зона: порог `maxNativeTableCells` → island-таблица (`replaceTableIsland`); смежный известный хвост — перенос широких ячеек Visual-grid.

## План спринтов

### 2. Корректность: round-trip и формулы — **ЗАВЕРШЁН 2026-07-18** (app 0.44.0)

- [ ] (наблюдение, НЕ баг-фикс) «Preview не перерендерился после удаления строки» — не воспроизведено; при повторении вернуть NSLog-цепочку `syncToDocument → updateNSView → render`. Перенесено в спринт-хвосты.

### 3. Темы, этап 2 — чистка **ЗАВЕРШЁН 2026-07-18** (app 0.45.0)

Пользователь (2026-07-18): «старую удали, потом сделаем пресеты для source и visual». Полная унификация через ThemeSpec отложена — сделана чистка.

- [ ] **Позже:** полноценные пресеты для Source и Visual поверх этого baseline (возможно ThemeSpec + проекции). Единый пикер, `ElementStyles`/`fontFamily` fallback’и в тему, «Reset to theme».

заметки на полях

- [ ] Кнопку commit приходится нажимать два раза.
- [ ] сделай кнопку push - серой. done синей и поменяй местами
- [ ] в сплит режиме панель инструментов может быть не свернутой, и может быть на весь экран, в том числе и над review частью.
- [ ] в сплит режиме когда экран с прокруткой и печатаешь на последней строке - экран source дергается.
- [ ] в сплит и source режиме хочется чтобы нажимая enter если ты работаешь со списками и чекбоксами они вставлялись автоматически. наверное и с таблицами также
- [ ] надо встроить логирование. потому что вот сейчас я это пишу и проценты приближаются к 100% загрузки процессора.
- [x] При запуске запускается indexing links и при открытие нового файла процесс начинается с 0. и по мере редактирования файла - тоже обнуляется. — Сделано 2026-07-19 (72300f6: same-key переиспользует идущий скан, linkEpoch вместо инвалидации на каждую активацию; 7b9685b: resolve-кэш; 2ae0064: прогрев на старте). Детали — docs/HISTORY.md, раздел «CPU-сага LinkIndex/vault-lint».
