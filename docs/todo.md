# TODO

Задачи сгруппированы по зоне кода, чтобы выполнять группами за один заход.

> Граф знаний кодовой базы: `graphify-out/` (4174 узла, 159 сообществ; `graph.html` — интерактив). Для структурных вопросов «что с чем связано» — `graphify query "…"` вместо ручного обхода. NB: рантайм-семантику SwiftUI (view-identity `.id`, teardown) граф не показывает — только типы/функции/вызовы.

## Группа A — Сайдбар и поиск по workspace

Зона: `WorkspaceSidebar` / `WorkspaceSearchSidebar` / `WorkspaceSearchModel` / `SearchMatch.swift`.

- [x] Files-сайдбар: выбор файла из середины списка при открытии скачком прокручивал сайдбар к началу папки; сбрасывались `selectedFiles`/`filterText` (2026-07-18). **Корень:** `MainWindowView` вешал `.id(currentURL)` на `FileEditor`, при каждом открытии уничтожая и пересоздавая всё поддерево вместе с `WorkspaceSidebar` → `ScrollView` терял offset. `.id` нужен для acquire/release документа в `DocHost`. **Сделано 2026-07-19 (подход 2 «hoist», внешнее ревью подтвердило разрез):** сайдбар вынесен из `.id`-свопаемой ветки в стабильный `MainChrome` (`FileEditor.swift`) — chrome живёт, `.id`-свопается только центр. Схлопнуты 4 копии сайдбара (ContentView / PDFViewerView / FolderInfo / WelcomeView) в одну; `sidebarVisible`/`sidebarWidth` + divider + toolbar-toggle + `focusedSceneValue(\.sidebarVisible)` — теперь один раз в `MainChrome`; `onOpen`-модалка «уже открыт в другом окне» вынесена в свободную `openFileFromWorkspaceSidebar`. Clamp разделён по сторонам (main: sidebar; ContentView: inspector). Инспектор остался в ContentView (document-scoped). Lite-окна (`allowsSidebar:false`) без `MainChrome` — как раньше. `DocHost`/undo/registry НЕ тронуты. ScrollViewReader из смягчения оставлен как safety-net (anchor `nil` — подскролл только для off-screen активного файла). Сборка + 1126 XCTest + 73 Swift Testing зелёные. **Внешнее ревью прошло; проверено глазами 2026-07-19 — OK.** Ревью-фиксы (5726327): divider на named coordinate space (`.global` был screen-relative → ширина уезжала на не-flush окне); мёртвый `allowsSidebar` убран из PDFViewerHost/ImageViewerHost; асимметрия single-side clamp задокументирована (сжатие не делится 50/50 при обеих панелях near-max, overlap нет). UX-сдвиг (full-height sidebar, status-bar только под центром, Xcode-like) — принят.
- [x] Поиск по workspace: горячие следы (`_opaqueCharacterStride`, `String.lowercased`, `Substring.index(before:)`) — перемерить на чистом волте, прежде чем оптимизировать. — **Сделано 2026-07-20.** Файл на самом деле `EditMD/Editor/SearchMatch.swift` (в todo был путь `Views/`), и он **в offline-таргете `editmdctl`** → правка Foundation-only, заодно ускоряет `editmdctl search`. **Профиль (standalone-бенч, WoL-волт 7010 md / 50 МБ, whole-body скан по одному токену):** текущий `range(of:options:.caseInsensitive)` = **3–6.8 с на токен** (vitamin 3976 / mitochondria 4937 / the 2991 / витамин 5778 / клетка 6778 мс). Case-**sensitive** без фолдинга ≈ 0.65–1.0 с (фолдинг = ~4–5×), остальное — grapheme-cluster итерация Foundation. **Вывод: НЕ ловушка, оптимизация реальна и крупная** (прежний вывод 2026-07-19 «двойной проход не трогать» верен — но истинная стоимость это whole-body casefold, и она катастрофична). Двойной фолд платят только совпавшие файлы (их мало): `guard matched` (:223) отсекает не-совпавшие до line-scan. **Решение: scalar-fold примитив** — ручной simple-fold по Unicode-скалярам (`searchFoldScalar`: ASCII A–Z, Latin-1 À–Þ, кириллица А–Я + Ё–Џ), остальные скаляры проходят case-sensitive (документировано в коде). Байтовый ASCII-fold отвергнут — ломает кириллицу. `searchContentMatches` фолдит тело/имя/needles по одному разу (не по разу на needle) и гоняет `searchFoldedContains`/`searchFoldedOccurrenceCount`. **Паритет с Foundation 100%** по всем needle на волте, вкл. смешанный регистр кириллицы (`Витамин`==`витамин`=1248, `КЛЕТКА`==`клетка`=570). **Скорость: ~10–17 мс на токен** после fold + 255 мс однократный fold волта → ~270 мс на запрос против 4–13 с = **15–45× быстрее**, корректность сохранена. Тесты: `testScalarFoldContainsCaseAndScript`, `testScalarFoldOccurrenceCountNonOverlapping`, `testScalarFoldContentMatchCyrillicMixedCase` + существующие `testCaseInsensitiveLatinAndCyrillic`/`testPhraseAcrossTokenBoundary`/`testLineHitUTF16Offset` зелёные. Полный suite 1133 XCTest зелёный. Глазами в живом app не проверено (бенч standalone на волте).

## Группа B — Layout и toolbar в сплит-режиме

Зона: геометрия сплитов `ContentView`/инспектор, toolbar сплита.

- [x] Layout при малой ширине окна: правый инспектор наползал на соседние панели, границы панелей перекрывались (2026-07-18). — Сделано 2026-07-19: `resolveSidePaneWidths` + `preferredPaneWidthFromDrag`; floor окна `mainWindowMinWidth` **900** / lite 560 через `NSWindow.contentMinSize` (`applyWindowContentMinimum` — SwiftUI `.frame(minWidth:)` на `Window` не держит live resize). 720 на практике ещё mid-word-wrap при двух панелях 220. Тесты: `PaneLayoutTests`.
- [x] В сплит-режиме панель инструментов может быть не свёрнутой и может быть на весь экран, в том числе и над review-частью. — Сделано 2026-07-20 (решение: «единая панель»). Место: `EditorActionStrip`. **Две правки:** (1) полоса получила сплошной фон-бар (`stripBar` = `Rectangle().fill(.bar)` — стандартный material toolbar, сам трекает light/dark) на всю ширину, так что инструменты (слева) и переключатель режимов (у дальнего края, над Preview) читаются как одна намеренная toolbar, а не два «плавающих» кластера с дырой. (2) Убран клап `laneInsideEditor` из `toolLaneWidth` — раньше лоток инструментов жёстко обрезался шириной Source-панели, из-за чего группы схлопывались в «…», хотя половина бара пустовала. Теперь `toolLaneWidth = laneBeforeMode` (он уже резервирует место под переключатель → перекрытия нет); инструменты текут по всему бару, в «…» уходят только при реально узком окне. В Source/Visual/Preview поведение не изменилось (там `laneBeforeMode ≤ laneInsideEditor` и так был binding). Нижний hairline сначала добавили, по фидбэку убрали. Проверено глазами 2026-07-20 — OK.
- [x] В сплит-режиме когда экран с прокруткой и печатаешь на последней строке — экран Source дёргается. — **Закрыто 2026-07-20 как «нет бага» (пользователь проверил вживую).** Уже покрыто демпфером `isMinorLayoutDrift` (`SplitScrollSync.swift:32`, окно ≤4px) из прошлых коммитов sync-спринта; на чистом сценарии репро не воспроизводится. Локализация 2026-07-19 (caret-follow `.visibleParagraphAnchor` ↔ anchor-follow `.scrollViewport`) оставлена как справка на случай регресса.

## Группа C — Редактирование в Source / Visual

Зона: editor input, native/island-таблицы, авто-продолжение списков.

- [ ] Visual: не работает редактирование больших таблиц (2026-07-18, найдено при проверке CPU-фикса LinkIndex на WoL-вольте). Уточнить репро: размер таблицы и что именно не работает (ввод в ячейку? сериализация?). Вероятная зона: порог `maxNativeTableCells` → island-таблица (`replaceTableIsland`); смежный известный хвост — перенос широких ячеек Visual-grid.
- [ ] В сплит- и Source-режиме: при нажатии Enter внутри списков и чекбоксов автоматически вставлять продолжение (маркер/`- [ ]`). Наверное и с таблицами так же.

## Группа D — Git-панель UI

Зона: Git-сайдбар.

- [x] Кнопку commit приходится нажимать два раза. — Сделано 2026-07-20. Место: **Git-сайдбар**. Репро: open с 1-го, Commit дважды (первый раз после входа на вкладку; после смены вкладки снова). **Две независимые причины:** (1) `acceptsFirstMouse` у SwiftUI `Button` → Diff/Commit/Push/Refresh на `.onTapGesture`. (2) **главное, осталось после (1):** dual-state sheet `showCommit` + optional `commitURL` — content builder мог открыть ветку «No file» и `onAppear` сразу dismiss; первый клик = no-op. **Фикс sheet:** `.sheet(item: $commitTarget)` / `diffTarget` (`GitSidebarCommitTarget` / `GitSidebarDiffTarget`) — payload и presentation одним присваиванием. **Smoke:** Git → Commit с 1-го; Files→Git → снова с 1-го; Diff тоже.
- [x] Кнопку push сделать серой, done — синей, и поменять их местами. — Сделано 2026-07-20: в `GitCommitSheet.successBody` (после коммита) Push теперь вторичная (серая, слева), Done — primary (`.defaultAction`, синяя, справа). Ревью-фикс: Done как default забрал Return, поэтому Escape привязан к скрытой Cancel-кнопке в `.background` (тот же механизм, что в `commitBody`). Nit (глазами): если Push визуально слишком похож на Done — добавить `.buttonStyle(.bordered)` Push vs `.borderedProminent` Done. **Smoke D1 (hit-test — обязательно глазами):** (1) Commit на неактивной строке → sheet с 1-го клика; (2) клик по имени файла (не по кнопке) → open; (3) Diff с 1-го клика; (4) context menu без регрессий. Если (2) сломан — fallback: outer-Button на всю строку или onTapGesture только на label-зону.
- [x] Git-список: не показывать удалённые / missing / trashed. — Сделано 2026-07-20: filter `status != .deleted` **и** `FileManager.fileExists` (trashed остаются porcelain-D, но path уже нет); то же для `openDirty` и `patchOpenDirtyMarks`. Тесты: `testWorkspaceSnapshotOmitsDeletedMarkdown`, `testWorkspaceSnapshotOmitsMissingOpenDirty`.
- [x] Удаление файла из приложения (context menu). — Сделано 2026-07-20: `confirmAndMoveFilesToTrash` (`FileMoveActions.swift`) — confirm → `FileManager.trashItem`, refresh workspace/git, close main if open. Пункт **Move to Trash** в Git-sidebar + Files (file/nested/loose rows); multi-select в Files через `moveFiles(anchoredAt:)`.
- [x] Общий commit на секцию репо (все md в папке/repo). — Сделано 2026-07-20: header **Commit all** рядом с Push; `GitCLI.commit(files:)` + multi-path sheet (`GitCommitSheet(fileURLs:)`). Только markdown из списка секции (не весь non-md tree). Тест: `testCommitMultipleFilesInOneCommit`.

## Группа E — Инфраструктура и диагностика

- [ ] Встроить логирование — сейчас при печати проценты приближаются к 100% загрузки процессора, нужен инструмент, чтобы это ловить.
- [ ] (наблюдение, НЕ баг-фикс) «Preview не перерендерился после удаления строки» — не воспроизведено; при повторении вернуть NSLog-цепочку `syncToDocument → updateNSView → render`.

## Группа F — Темы (отложено)

Пользователь (2026-07-18): «старую удали, потом сделаем пресеты для source и visual». Старая система выбора удалена (спринт 3, app 0.45.0). Полная унификация через ThemeSpec отложена.

- [ ] Полноценные пресеты для Source и Visual поверх baseline (возможно ThemeSpec + проекции). Единый пикер, `ElementStyles`/`fontFamily` fallback’и в тему, «Reset to theme».