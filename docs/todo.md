# TODO

## Низкоприоритетный техдолг

- Native-таблицы Visual: в пустую ячейку нельзя попасть кликом мимо точной позиции каретки (левый верхний угол); ячейки с переносами ловятся везде.
- Copy из Visual кладёт display-текст токенов (☑ вместо `[+]`, U+FFFC) — источник утёкших ☑ при вставке обратно. Кандидат на отдельный спринт (responder chain, частичные выделения).
