# Демо: формулы

Встроенная формула: $E = mc^2$ прямо в тексте.

TeX с подчёркиваниями и бэкслэшами: $a_i + \frac{x}{y}$ — cmark их не калечит.

Греческие буквы и операторы: $\alpha \cdot \beta \le \gamma$.

## Блочные формулы

$$
\int_0^\infty e^{-x^2}\,dx = \frac{\sqrt{\pi}}{2}
$$

Матрица:

$$
\begin{pmatrix} a & b \\ c & d \end{pmatrix}
\begin{pmatrix} x \\ y \end{pmatrix}
=
\begin{pmatrix} ax + by \\ cx + dy \end{pmatrix}
$$

Однострочный display: $$\sum_{n=1}^{\infty} \frac{1}{n^2} = \frac{\pi^2}{6}$$

## Формулы в контексте

- В списке: $x^2 + y^2 = z^2$
- В **жирном**: **важно $\lim_{x \to 0} \frac{\sin x}{x} = 1$**

> Цитата с формулой $e^{i\pi} + 1 = 0$ (инлайн работает; многострочный $$-блок в цитате — нет).

| Формула | Значение |
| --- | --- |
| $\pi$ | 3.14159 |
| $e$ | 2.71828 |

## Не формулы

Цены $20 и $30 — валюта, не математика.

Экранированный \$x\$ — тоже не формула.

Внутри `кода $x$` и в блоке:

```
$$a + b$$
```

— доллары остаются как есть.

Синтаксическая ошибка рендерится красным: $\frac{a$
