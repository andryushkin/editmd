.DS_Store
*.xcuserstate
xcuserdata/
DerivedData/
.build/
*.baseline
.smotr-queue.json
.smotr-agent.log
.claude/
__pycache__/
*.py[cod]
EditMD/build/
/dist/

# graphify knowledge-graph output (generated, local-only)
graphify-out/

# local research clones of third-party editors (not part of the project);
# anchored so docs/research/ stays tracked
/research/

# Icon generator intermediates
design/build/

# Private working notes; never part of the repository.
/DOTMD.md

# Prebuilt core library: 277 MB, built elsewhere and copied in.
# `Vendor/core.expected.json` next to it stays tracked — it is what
# scripts/verify-core.sh checks the artifact against.
/Vendor/PrintDotMD.xcframework/
